# include <iostream>

using namespace std;

int main()
{
    int a[10];
    int n, min, max;

    cin >> n;

    for (int i = 0; i < n; i++) {
        cin >> a[i];
    }

    for (int i = 0; i < n - 1; i++) {
        if (a[i] > a[i + 1])
        {
            int temp = a[i];
            a[i] = a[i + 1];
            a[i + 1] = temp;
        }
        else
            break;
    }
    for (int i = 0; i < n; i++)
        cout << a[i] << endl;
    return 0;
}
